{
	"name": "leme-writing-studio",
	"displayName": "LeME Writing Studio",
	"description": "This is an extension that supports writing your ebook.",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.61.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:leme-writing-studio.preview",
		"onCommand:leme-writing-studio.createBook",
		"onCommand:leme-writing-studio.selectBook",
		"workspaceContains:*.leme"
	],
	"main": "./out/extension.js",
	"contributes": {
		"menus": {
			"editor/context": [
				{
					"when": "editorTextFocus && resourceExtname == .txt",
					"command": "leme-writing-studio.preview",
					"group": "leme-writing-studio@0"
				}
			]
		},
		"commands": [
			{
				"command": "leme-writing-studio.preview",
				"title": "LeME: Preview"
			},
			{
				"command": "leme-writing-studio.createBook",
				"title": "LeME: Create a new book(*.leme)"
			},
			{
				"command": "leme-writing-studio.selectBook",
				"title": "LeME: Select a book(*.leme)"
			}
		],
		"configuration":[
			{
				"title": "LeME Writing Studio",
				"properties": {
					"selected-book-name": {
						"type": "string",
						"default": null,
						"description": "Books that has the last opened documents.",
						"scope": "resource"
					}
				}
			}
		],
		"keybindings":[
			{
				"command": "skipRight",
				"key": "ctrl+right",
				"when": "textInputFocus && !accessibilityModeEnabled"
			},
			{
				"command": "skipRightSelect",
				"key": "ctrl+shift+right",
				"when": "textInputFocus && !accessibilityModeEnabled"
			},
			{
				"command": "skipLeft",
				"key": "ctrl+left",
				"when": "textInputFocus && !accessibilityModeEnabled"
			},
			{
				"command": "skipLeftSelect",
				"key": "ctrl+shift+left",
				"when": "textInputFocus && !accessibilityModeEnabled"
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile && npm run lint",
		"lint": "eslint src --ext ts",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.1.3",
		"@types/mocha": "^8.2.2",
		"@types/node": "14.x",
		"@types/vscode": "^1.61.0",
		"@typescript-eslint/eslint-plugin": "^4.26.0",
		"@typescript-eslint/parser": "^4.26.0",
		"eslint": "^7.27.0",
		"eslint-plugin-jest": "^25.2.4",
		"glob": "^7.1.7",
		"jest": "^27.3.1",
		"mocha": "^8.4.0",
		"typescript": "^4.3.2",
		"vscode-test": "^1.5.2"
	}
}
